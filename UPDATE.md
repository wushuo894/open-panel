# 自 v1.0.2 以来的更新

## 新增

- Open Panel 的 Docker 部署支持在关于页面校验并安装 Release JAR，更新后由容器启动脚本自动重启 Java 进程。
- 卡片图标支持按卡片独立开启无框显示，图片图标可使用完整图标区域并自动应用自适应圆角。

## 优化

- Docker 管理页及镜像更新检测任务不再显示 Open Panel 自身容器。

## 修复

- 修复下载 GitHub Release 更新文件时未跟随 HTTP 302 重定向，导致更新失败的问题。
