# 自 v1.0.3 以来的更新

## 新增

- 清理 Docker 镜像前会先列出所有待清理镜像及大小，确认后才执行删除。
- 配置导入导出改为 ZIP 备份，可同时迁移上传的背景图和卡片图标；导入时会校验 ZIP 结构与图片内容。
- 搜索框支持单独隐藏，卡片与搜索框可统一调整透明度和圆角。

## 优化

- Docker 管理页将检测到可更新的容器置顶显示。
- 前端生产构建使用 `vite-plugin-compression` 生成 Brotli 与 gzip 资源，服务端根据浏览器能力自动返回预压缩文件。
