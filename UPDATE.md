# 自 v1.0.1 以来的更新

## 新增

- 新增基于 VitePress 的独立中文文档项目，覆盖部署、配置、安全、卡片、Docker 管理和更新说明。
- 卡片组新增 Tabs 布局，可通过“全部”或分组标签筛选当前显示的卡片；“全部”标签可按需关闭，并保留各分组原有的详细或仅图标模式。
- 外观设置新增自定义主题色，内置 23 个推荐色，支持颜色选择、十六进制输入，并可从当前壁纸自动选取具有较好辨识度的颜色。
- 设置中新增 Docker 页面，仅在检测到 Docker 可用时显示；项目启动后自动检查一次容器镜像更新，之后每小时检查一次。
- Docker 页面支持启动、停止和重启容器，并可查看、复制根据容器当前配置生成的 `docker-compose.yaml`。
- Docker 容器列表显示实时运行时长。
- 支持清理未被现有容器使用的镜像，并显示删除数量与释放空间。
- 支持随时手动一键更新容器镜像，无需等待检测到新镜像；自动任务仅检测更新，不会自动重建容器。更新过程会展示进度与操作日志，镜像拉取阶段可以中断。
- Open Panel 的 Docker 部署支持在关于页面校验并安装 Release JAR，更新后由容器启动脚本自动重启 Java 进程；Docker 管理页不再显示 Open Panel 自身容器。

## 优化

- 容器更新时先拉取新镜像，并使用完整镜像 Hash 暂存；原标签在进入替换阶段前始终保留给当前运行镜像。
- 替换容器时依次停止并删除旧容器、为旧镜像添加 Hash Tag、恢复新镜像原标签，再使用原配置重建容器。
- 重建容器时保留环境变量、数据卷、端口、网络、标签和重启策略；更新失败时自动恢复旧镜像和容器。
- 容器替换阶段禁止中断，避免容器停留在配置不完整的中间状态。
- 启动时、每小时及手动检查更新时仅查询远端镜像 Digest，不再拉取镜像或修改本地标签；只有点击容器更新按钮才拉取镜像。
- Docker 容器列表改为紧凑的管理面板卡片，使用状态点、单行摘要和图标工具栏展示容器信息与操作；更新中的容器使用细进度条展示当前阶段，并可在镜像拉取阶段直接停止更新。
- 将站点与页面区域的两个开关合并排列，减少设置项之间的割裂感。
- 优化文档站夜间模式的文字、代码块、表格和提示框对比度。

## 修复

- 修复手动更新容器后当前镜像变成 `sha256:...`，导致镜像名称丢失并被错误标记为不可更新的问题。
- 已出现该问题的容器会从更新暂存记录或镜像 RepoTags 恢复仓库名与 Tag，再次手动更新后会将正确镜像名写回容器配置。
- 修复列表模式内容不足一屏时页脚下方出现大块空白的问题，页脚现在会稳定贴合视口底部。

## 限制与保护

- 当前 Open Panel 容器、固定 Digest 镜像、Swarm 容器及自动删除容器不提供一键更新，避免破坏现有部署。
- 清理镜像仅删除未被现有容器引用的镜像。
