# 自 v1.0.0 以来的更新

## 新增

- 软件服务新增 OpenList 类型，可通过 API Token 获取存储信息，并在卡片中展示存储总容量和存储数量。
- 软件服务与 Docker 卡片支持从内网或公网链接自动补全标题、简介和网站图标。
- 选择 Docker 容器时，可使用容器名称自动同步卡片标题。
- 切换系统信息指标时，自动同步系统信息、CPU、内存、存储或网络标题。
- 首页拖动卡片时增加实时占位预览，支持卡片前后位置、分组末尾及跨分组移动。

## 优化

- 重新整理卡片编辑弹窗，将所属分组、卡片类型和打开方式归为一组，将标题和备注归为一组。
- 统一 MDI 图标、图片链接和本地上传的配置入口，增加当前图标预览，并保留不同来源已经填写的内容。
- 切换卡片类型时保留原有标题、备注以及内外网 URL，避免编辑内容被默认值覆盖。
- Docker 卡片高度与普通卡片保持一致，移除 CPU 和内存占用展示，保留运行状态与运行时间。
- Docker 启动、停止和重启操作改为卡片悬停时在右下角显示，降低按钮的视觉干扰；触屏设备保持可操作。
- GitHub Release 自动读取本文件作为版本更新说明。

## 修复

- 修复部分正常网站在服务状态检测中错误显示“连接失败”的问题。
- 修复部分网站自动补全时报错 `HTTP/1.1 header parser received no bytes` 的问题，相关请求改为使用 HTTP/1.1。
- 修复 OpenList 服务类型和 API Token 保存后被还原或清空的问题。
- 修复首页编辑状态下导航区域出现多余横线的问题。
