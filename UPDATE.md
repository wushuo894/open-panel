# 自 v1.0.7 以来的更新

## 优化

- 首页卡片增加右键菜单，可按默认、内网或公网地址打开和复制链接，登录后可直接编辑或删除卡片。
- 首页卡片编辑状态保留拖拽排序、添加卡片和右上角快捷删除，并移除容易遮挡内容的编辑操作层。
- 首页卡片组悬停时在名称旁显示快捷添加按钮，可直接向当前分组添加并独立保存卡片，无需进入排序状态。
- 缩小卡片右键菜单；右键编辑与删除改为独立保存，不再自动进入首页卡片排序状态。
- 卡片排序状态增加轻微摆动提示，仅在实际拖动卡片或启用减少动态效果时停止动画。
- 登录后后台预取卡片管理配置，新增和编辑弹窗立即响应；非 Docker 卡片不再额外请求容器列表。
- 卡片及右键菜单文字禁止鼠标选中，并加快排序状态下的轻微摆动速度。
- 卡片组悬停时显示的添加按钮移动到标题栏右侧，Banner 默认开启精确到秒。
