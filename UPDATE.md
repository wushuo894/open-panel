# 自 v1.0.6 以来的更新

## 新增

- Docker 未使用镜像清理支持按需选择镜像，并提供全选、已选数量和预计释放空间统计。

## 优化

- 镜像清理改为按所选镜像逐个执行，并在结果中显示成功删除和跳过的镜像数量。
- Docker 容器更新使用 12 位镜像短摘要作为临时 Tag，并兼容识别旧版 64 位 Hash Tag。
- 修复容器更新时把 Docker 动态分配的运行时 IP 当作静态地址恢复，导致默认 bridge 重建和自动回滚失败的问题；自定义网络仅恢复显式 IPAM 配置。
- 修复生成 docker-compose.yaml 时遗漏内置 bridge 网络模式的问题，并避免 `network_mode` 与 `networks` 同时输出。
- 修复生成 docker-compose.yaml 时列表项没有相对字段缩进的问题。
