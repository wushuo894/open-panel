# v1.0.10 更新内容

## 优化

- Web 服务扫描结果改为在独立弹窗中展示，扫描完成后自动打开，也可从扫描进度区域再次查看；弹窗顶部选择区和结果列表采用紧凑自适应布局。
- 扫描结果支持全选、取消全选和已选数量统计；添加服务前必须由用户手动选择并确认目标分组，避免误加到默认分组。
- Docker 镜像清理现在会区分完整的未使用镜像与仍被容器使用镜像上的冗余标签，并分别显示清理数量。
- 镜像清理列表使用独立清理目标进行选择，准确显示冗余标签、镜像摘要和预计释放空间。

## 修复

- 修复多个镜像标签指向同一镜像时，冗余标签无法被识别和单独清理的问题。
- 修复容器更新完成后临时镜像标签可能清理失败且没有日志提示的问题。
- 调整 Maven 依赖版本管理并补齐编译期注解依赖，提高项目构建兼容性。
